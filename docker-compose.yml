services:
  mysql-db:
    image: 'mysql:8.0'
    environment:
      - 'MYSQL_DATABASE=code_shop_db'
      - 'MYSQL_PASSWORD=password'
      - 'MYSQL_ROOT_PASSWORD=root_password'
      - 'MYSQL_USER=user'
    ports:
      - '3306:3306'